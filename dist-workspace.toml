[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# This fork ships Windows and Linux only, as development-channel builds: the
# upstream's official-build marker, macOS signing, build-channel check and
# telemetry contract are alphaXiv-only and were removed. release.yml is still
# hand-edited, so dist skips it; see macos/DISTRIBUTION.md before changing
# dist config.
allow-dirty = ["ci"]
# Run source CI before publishing.
global-artifacts-jobs = ["./ci"]
# The installers to generate for each app
installers = ["shell", "powershell"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-unknown-linux-musl", "x86_64-unknown-linux-musl", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
# Trigger releases via workflow_dispatch instead of tag push. The tag is
# created implicitly when the GitHub Release is published at the end, so no
# workflow ever pushes a tag — which lets release-on-bump.yml dispatch
# releases with the default GITHUB_TOKEN (workflow_dispatch is exempt from
# GitHub's "GITHUB_TOKEN events don't trigger workflows" rule; tag pushes are
# not). No PAT or extra secret required.
dispatch-releases = true
